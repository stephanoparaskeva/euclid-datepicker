import { DatePickerProps } from '.';
declare const DatePickerIOS: ({ style, date, locale, maximumDate, minimumDate, mode, minuteInterval, timeZoneOffsetInMinutes, textColor, onDateChange, }: DatePickerProps) => any;
export default DatePickerIOS;
